# frontend

Web dashboard for:

- Session monitoring (`/v1/proto/sessions`)
- Protocol metrics (`/v1/proto/metrics`)
- Event/log stream (`/v1/proto/events`)
- Connect/disconnect profile controls (`/v1/connect`, `/v1/disconnect`)
